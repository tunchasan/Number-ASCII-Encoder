#ifndef MATH_H
#define MATH_H

struct Math {

public:

	// Obeb Operation
	static int OBEB(int, int);

	// Modulo Operation
	static int MOD(int, int);

	// Compare Operation
	static int MAX(int, int);

	// Compare Operation
	static int MIN(int, int);
};

#endif
